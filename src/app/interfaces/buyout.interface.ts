export interface IBuyout {
  _id?: number;
  // eslint-disable-next-line @typescript-eslint/naming-convention
  _id_category: number;
  description: string;
  amount: number;
  badge: string;
  month: string;
  // eslint-disable-next-line @typescript-eslint/naming-convention
  create_at: string;
}
