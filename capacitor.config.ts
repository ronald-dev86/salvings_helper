import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.salvigshelper.com',
  appName: 'salvings helper',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
